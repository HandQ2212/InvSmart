# Optional. If omitted, firebase-admin will read GOOGLE_APPLICATION_CREDENTIALS.
SERVICE_ACCOUNT_PATH=C:/path/to/service-account.json

# Optional. If omitted, the script uses the project_id from service account.
FIREBASE_PROJECT_ID=your-project-id
